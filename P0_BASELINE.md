# P0 パフォーマンス基線

| 指標 | 改善前の観測値 | 判断 |
| --- | ---: | --- |
| メインJavaScript | 1,358,685 bytes（約1.36 MB） | ホーム初回表示に不要な画面コードが同梱されている。ルート単位の遅延読込が必要。 |
| 七月場所アーカイブチャンク | 444,982 bytes | アーカイブデータは分離済みだが、画面コンポーネントの分離を進める余地がある。 |
| CSS | 77,959 bytes | 今回は主要課題ではない。 |
| ルーティング | `app/main.tsx`が主要ページを静的import | `React.lazy`＋`Suspense`へ移行する。 |
| ホーム状態導線 | 状態表示はあるが、ヒーローのCTAは状態別に主従が変わらない | `basho-status`から状態別の主CTA／補助CTAを組み立てる。 |

計測日は 2026-08-15。チャンクサイズは`dist/assets`の生成物をバイト単位で取得した値であり、ネットワーク転送サイズやキャッシュ状態は含まない。

## 実装後の観測値

| 指標 | 実装後の観測値 | 改善前との差分 |
| --- | ---: | ---: |
| メインJavaScript | 1,313,117 bytes（約1.31 MB） | 45,568 bytes減少。 |
| 比較画面チャンク | 4,760 bytes | 初回に不要な比較UIを分離。 |
| マイ力士画面チャンク | 3,350 bytes | 初回に不要な保存済み一覧UIを分離。 |
| プロフィール詳細チャンク | 4,696 bytes | 詳細ページUIを分離。 |
| トップレベル詳細チャンク | 10,266 bytes | 日別詳細の画面コードを分離。 |
| CSS | 46,947 bytes | 画面CSSもルート単位で分離され、初回CSSは減少。 |

`index`チャンクには既存の大規模データ／共通ロジックが残るため、500KB警告は継続している。今回の変更では、比較・マイ力士・プロフィールなどの追加機能を初回ロードへ同梱しないことを達成した。次の速度改善候補は、既存の共通データ依存をさらに分解することである。

## 最終P0分割結果

| 指標 | 最終値 | 改善前との差分 |
| --- | ---: | ---: |
| 初回エントリJavaScript | 386,344 bytes（gzip 121.80 kB） | 972,341 bytes削減（約71.2%減）。 |
| 歴史アーカイブデータ | 927,117 bytes（gzip 41.96 kB） | 三月・五月の大量データを個別のキャッシュ可能チャンクへ分離。 |
| 七月場所アーカイブデータ | 444,982 bytes（gzip 19.33 kB） | 既存の分離を維持。 |

500KB警告は歴史アーカイブデータチャンクに対して残るが、ホームのエントリチャンクからは除去された。これは、初回画面のJavaScript実行・解析負荷を直接減らしつつ、アーカイブ画面では必要なデータを保持する分割である。
